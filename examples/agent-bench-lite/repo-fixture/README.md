# AgentBench-lite Repo Fixture

This directory is intentionally small. The deterministic fake provider does not
execute or modify these files; tickets reference them so benchmark scenarios
have stable paths for CLI, validation, documentation, forbidden CI, and eval
weakening cases.
